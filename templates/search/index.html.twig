{% extends 'base.html.twig' %}

{% block title %}
    
{% endblock %}

{% block body %}

<div id="section">
    {% if app.request.method == 'POST' %}
        <div>
            {% if voitures | length == 0 %}
                <h4>Aucun résulat pour votre recherche.</h4>
            {% else %}
                <div class="voiture-content content ">

                    <div class="column left">

                        <div id="filterMenu">
                            {% include "voitures/partials/_filterMenu.html.twig" %}
                        </div>

                    </div>
                    <div class="column right">
                
                        {# ------------Voiture show ------------ #}
                        <div class="voitureCard">
                            {% include "voitures/partials/_showVoitures.html.twig" %}
                        </div>

                    </div>

                </div>

            {% endif %}
        </div>
    {% endif %}
        {# ------------footer show Index------------ #}
    <footer class="site-footer pv-medium animated">
        {% include "/partials/_footer.html.twig" %}
    </footer>

</div>

{{ encore_entry_link_tags('show') }}
{% endblock %}