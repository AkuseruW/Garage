{% extends 'base.html.twig' %}

{% block title %}Voitures{% endblock %}

{% block body %}
    <h1>Voitures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>ModelName</th>
                <td>{{ voiture.modelName }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ voiture.prix }}</td>
            </tr>
            <tr>
                <th>Kilometrage</th>
                <td>{{ voiture.kilometrage }}</td>
            </tr>
            <tr>
                <th>Nbr_propi</th>
                <td>{{ voiture.nbrPropi }}</td>
            </tr>
            <tr>
                <th>Puissance</th>
                <td>{{ voiture.puissance }}</td>
            </tr>
            <tr>
                <th>Cylindre</th>
                <td>{{ voiture.cylindre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voiture.description }}</td>
            </tr>
            <tr>
                <th>Carburant</th>
                <td>{{ voiture.Carburant }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ voiture.date ? voiture.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ voiture.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voitures_index') }}">back to list</a>

    <a href="{{ path('app_voitures_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voitures/_delete_form.html.twig') }}
{% endblock %}
